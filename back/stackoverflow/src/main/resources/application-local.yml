spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:testdb;NON_KEYWORDS=USER
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
    #  security:
    # In-Memory Test
    # user:
    #  name: user
    #  password: user
#    oauth2:
#      client:
#        registration:
#          google:
#            clientId: ${GOOGLE_CLIENT_ID}
#            clientSecret: ${GOOGLE_CLIENT_SECRET}
#            scope:
#              - email
#              - profile
server:
  servlet:
    encoding:
      force-response: true
  error:
    include-exception: true
    include-stacktrace: always

logging:
  level:
    org.hibernate.SQL: trace
    org.hibernate.type.descriptor.sql.BasicBinder: trace

# Cors
#cors:
#  allowed-origins: "*"
#  allowed-headers: "*"
#  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
#  max-age: 3600

 #Auth & OAuth
#app:
#  auth:
#    tokenSecret: ${JWT_SECRET_KEY}
#    tokenExpiry: 1800000
#    refreshTokenExpiry: 604800000
#  oauth2:
#    authorizedRedirectUris:
#      - http://localhost/recieve

jwt:
  secret: temporary_secret_key_temporary_secret_key_temporary_secret_key_temporary_secret_key
  access-token-expiration-minutes: 1800000
  refresh-token-expiration-minutes : 604800000


mail:
  smtp:
    host: smtp.gmail.com
    port: 587
    username: seb42_pre22@gmail.com //Gmail email
    password: tenauoxdglfiktvs //Google 계정 2단계 인증 생성 후 앱 비밀번호 생성
    auth: true
    starttls:
      enable: true //일부 SMTP 서버에는 TLS 연결이 필요하므로 TLS 보호 연결을 활성화한다
  subject:
    member:
      registration: Thank you for joining our cafe!

